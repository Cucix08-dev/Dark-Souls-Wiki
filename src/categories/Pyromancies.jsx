import { useState } from 'react';

export default function Pyromancies({ setPage, setSelectedCategory, setSelectedGame }) {
  if ({setSelectedGame} === "I") {
    return (
      <>
          
      </>
    );
  }
  else if ({setSelectedGame} === "II") {
    return (
      <>
          
      </>
    );
  }
  else if ({setSelectedGame} === "III") {
    return (
      <>
          
      </>
    );
  }
  
}
