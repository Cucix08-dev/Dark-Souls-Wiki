import { useState } from 'react';

export default function Rings({ setPage, setSelectedCategory, setSelectedGame }) {
  if ({setSelectedGame} === "I") {
    return (
      <>
          
      </>
    );
  }
  else if ({setSelectedGame} === "II") {
    return (
      <>
          
      </>
    );
  }
  else if ({setSelectedGame} === "III") {
    return (
      <>
          
      </>
    );
  }
  
}
